<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor">
  <!-- Full moon: a clean disc that fills its (square) viewBox, so it never
       clips. Rendered square by game.ts so it stays circular. -->
  <circle cx="10" cy="10" r="10"/>
</svg>
